program NumberOfDigits;
vaR
  nUmber, count, a , b: Integer;
  skan,asf,as : real;
  asg,greg,hjer :   boolean
begin
  write("Enter a number:");
  read(number);
  b := 20;
  for a := 10 to 20 do
  begin
    count := 1;
  end;
end.
